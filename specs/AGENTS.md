# Feature Specifications Workflow

- **Spec First:** Create a Markdown spec in this directory (`specs/`) before implementation (e.g., `feature-name.md`).
- **Content:** Outline objectives, data models, and UI/UX flows.
- **Approval:** Wait for user confirmation of the spec before writing code.
